function XC_c_scan(ρ, norm∇ρ, τ)
  # TODO! we need to implement this according to paper
  # If we assume that ρ isn't spin polarized
  # ζ is 0
  rₛ = (3 / (4 * π * n))^3

  sc = 1

  return sc, v1c, v2c, v3c
end
